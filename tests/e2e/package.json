{
  "name": "pcc-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end auth flow tests against the running stack (Playwright).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
