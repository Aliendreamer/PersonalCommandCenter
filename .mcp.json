{
  "mcpServers": {
    "postgres": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "personalcommandcenter-postgres-mcp-1",
        "postgres-mcp",
        "--access-mode=unrestricted"
      ]
    }
  }
}
