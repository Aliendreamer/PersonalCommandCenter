# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This is a new, empty repository (no commits, no source yet). This file is a stub — the
sections below should be filled in as the codebase takes shape. Re-run `/init` once there
is real source, build configuration, and tests to document accurately.

## Project

**PersonalCommandCenter** — a personal command center / dashboard application (intent only;
stack and scope not yet decided).

## Commands

_To be added once a build system, linter, and test runner exist (build, lint, run, single test)._

## Architecture

_To be added once there is code spanning multiple files worth describing at a high level._
